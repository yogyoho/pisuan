---
name: template-recommender
description: "从领域知识库中智能搜索并推荐报告章节和段落模板。当用户需要编写工程类报告（如环评报告、可行性研究等）、查找报告章节结构、搜索段落模板时使用此技能。支持按章节标题递归搜索子章节并合并输出模板与原文引用。"
---

# 段落模板智能推荐

从用户对话设置中指定的领域知识库中，根据用户提问的关键字搜索匹配的章节和段落模板。若用户指定了具体章节（如"第四章 环境影响预测"），则递归搜索该章节及其所有子章节，将泛化模板与原文引用合并输出。

## 适用场景

- 用户需要编写某类工程报告的特定章节
- 用户提供了章节标题，需要获取该章节的标准模板结构
- 用户想了解某领域报告的章节组织方式和段落模板
- 报告智能编写流程的第一步——先获取模板，再填槽

## 操作流程

### 第一步：确认知识库范围

1. 调用 `list_kbs` 获取当前对话可用的知识库列表（这些 KB 由用户在对话设置中预先选定）
2. 若用户未指定知识库或可用 KB 为空，告知用户需先在对话设置中选择相关领域知识库

### 第二步：了解章节结构

1. 对每个相关 KB，调用 `get_mindmap(kb_name)` 获取该知识库的章节树结构
2. 从思维导图中理解报告的顶层章节组织、父子章节关系、章节编号体系
3. 若用户提到了具体章节编号或标题，在思维导图中定位该章节节点及其子树

### 第三步：取段落模板

调用 `get_templates(domain, report_type, canonical_chapter_key)` 获取该章结构化段落模板（直接含 `generalized_pattern`/`slots`/`sample_original`/`standard_code`）。按章节递归：先取本章模板，子章节用各自 `canonical_chapter_key` 再取。根据用户需求分两种调用策略：

**策略 A — 用户指定了具体章节（如"第四章 环境影响预测"、"4.1 大气环境"）：**
1. 将章节标题归一化为 `canonical_chapter_key`，调用 `get_templates(domain, report_type, canonical_chapter_key)` 取本章模板
2. 在思维导图中定位该章节节点，遍历其所有子章节（递归），对每个子章节用各自 `canonical_chapter_key` 再取模板
3. 确保该章节及其完整子树都被覆盖

**策略 B — 用户只给了关键字（如"大气环境影响"、"水资源"）：**
1. 在思维导图中按关键字定位匹配章节，归一化为 `canonical_chapter_key`
2. 对每个匹配章节调用 `get_templates` 取模板
3. 根据 `section_path` 将结果按章节层级组织

### 第四步：合并输出

按以下 Markdown 格式汇总输出：

## 输出格式

```markdown
# 模板推荐结果

## 知识库概览
- 搜索范围：{KB名称列表}
- 搜索策略：{策略A 章节递归 / 策略B 关键字搜索}

## 章节结构概览

采用思维导图缩进格式，标注 **已匹配** 的章节：

- 第一章 总论
  - 1.1 项目背景 **[已匹配]**
  - 1.2 编制依据
- 第二章 工程分析 **[已匹配]**
  - 2.1 生产工艺 **[已匹配]**
  ...

## 模板详情

### {章节路径} {章节标题}

**标准编码**: `{standard_code}`
**匹配置信度**: {confidence}
**模板ID**: `{template_id}`

#### 泛化模板
{generalized_pattern}

#### 插槽清单
| 插槽名 | 类型 | 描述 | 必填 |
|--------|------|------|------|
| {slot.name} | {slot.type} | {slot.description} | {is_required} |

#### 原文引用
> {原始段落文本}

---
（对每个匹配的章节/段落重复以上结构）
```

## 关键约束

- 知识库范围限定在用户对话设置中已选定的知识库，不要跨库搜索
- 若指定了章节，必须递归包含其所有子章节，不可遗漏
- 原文引用必须完整保留原始措辞，不可修改或概括
- 若同一章节在多个 KB 中有匹配，分别列出并标注来源 KB
- 搜索结果为空时，明确告知用户未找到匹配模板，并建议调整关键字或确认知识库范围
- 不要编造模板或原文内容，所有输出必须来自知识库检索结果
