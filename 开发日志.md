 
 
 
 领域知识工厂模块 — 设计合理性分析

  一、业务流程闭环分析

  当前 ETL 流水线是 半开环 的：数据从"上传"到"入库知识库"有完整链路，但存在多处断链。

  完整链路（已闭环）

  上传 → 解析(PARSING) → 提取(EXTRACTING) → 泛化(GENERALIZING) → 人工审核(WAITING_REVIEW) → 入库(COMMITTED)
                                                                                ↓
                                                                      知识库向量化 + Neo4j 图谱构建

  这条主链路设计合理：阶段清晰、异步执行、失败可重试、人工审核兜底。

  断链点

  1. 实体 Schema → 提取 → 未识别实体 → 新 Schema（断）

  ETL 的 _etl_pipeline_async 在泛化阶段用 EntityMetaMatcher 匹配实体，前端 EtlWorkbench 第 4 步展示"未识别实体"。但：
  - 后端 get_unrecognized_entities 只返回未匹配的插槽，没有自动建议新实体定义
  - 用户在前端保存新实体类型时调用的是 entityTypeApi.createEntityType()，但这个 API 的路由 entity_type_router
  已被删除（git status 显示 D backend/server/routers/section_routing_router.py），实体类型路由 entity_type_router 在
  __init__.py 中 import 但该文件可能不存在于最新代码中
  - 实体构建器（DomainEntityBuilderView）的 AI 提取和 ETL
  的未识别实体之间没有数据流——用户需要手动切换页面重新输入文档内容

  2. 泛化模板 → 模板库（断）

  ETL 为每个段落生成了泛化模板（generalized + slots），但：
  - 这些模板只存储在任务的 source_paragraphs JSON 中，不会回写到 backend/templates/ 模板库
  - TemplateMatcher 只从 JSON 文件加载预定义模板，处理了 100 份文档后仍然只能匹配最初的 30 个硬编码模板
  - 没有模板质量评估机制（哪个模板泛化效果好、哪个差）

  3. 章节树 → 章节路由配置（断）

  _build_section_tree_from_paragraphs 能提取文档的章节结构，_generate_writing_guidance 和 _generate_entity_bindings
  也能生成编写指导和实体绑定。但：
  - 这些产出只在 _reingest_pipeline_async 中生成，没有持久化到 DomainFactorySavedSection 表
  - import_saved_section 虽然存在，但前端没有对应的 UI 入口来管理和复用这些章节知识
  - section_routing_router 已被删除，章节路由功能断裂

  4. 入库知识 → Agent 使用（断）

  入库后数据去向：
  - LightRAG 向量化（可被 RAG 检索）
  - Neo4j 知识图谱（GraphBuilder）

  但 Agent 侧没有对接领域知识的专用逻辑：
  - 普通聊天走通用 RAG 检索，不知道领域工厂的结构化元数据（行业、报告类型、章节路径）
  - 知识图谱只有构建没有查询接口——没有 API 让 Agent 按模板、按实体、按章节路径检索
  - 泛化模板中的插槽（slots）是"写作模具"的核心价值，但没有任何 Agent 技能能利用它们生成新文档

  ---
  二、数据使用分析

  ┌─────────────────────────────────┬────────────────────────────┬──────────────────────────────────────────────────┐
  │              产出               │          当前去向          │                  是否被有效利用                  │
  ├─────────────────────────────────┼────────────────────────────┼──────────────────────────────────────────────────┤
  │ 结构化基础信息（base_info）     │ 入库 Markdown 的表格       │ 仅作为向量检索的文本，失去结构                   │
  ├─────────────────────────────────┼────────────────────────────┼──────────────────────────────────────────────────┤
  │ 段落 + 泛化模板                 │ StructuredDocument →       │ 模板和插槽作为 metadata                          │
  │                                 │ LightRAG                   │ 存储，但检索时无法按模板/插槽过滤                │
  ├─────────────────────────────────┼────────────────────────────┼──────────────────────────────────────────────────┤
  │ 结构化表格（structured_blocks） │ 入库 Markdown 的表格       │ 退化为文本，表格关系丢失                         │
  ├─────────────────────────────────┼────────────────────────────┼──────────────────────────────────────────────────┤
  │ Neo4j 知识图谱                  │ Document/Section/Slot 节点 │ 只有写没有读——没有图谱查询 API                   │
  ├─────────────────────────────────┼────────────────────────────┼──────────────────────────────────────────────────┤
  │ 实体 Schema                     │ 提取阶段的变量定义         │ 仅用于输入，处理结果不反馈更新 Schema            │
  ├─────────────────────────────────┼────────────────────────────┼──────────────────────────────────────────────────┤
  │ 模板匹配结果                    │ 段落的 template_match 字段 │ 仅用于展示，不回写模板库                         │
  └─────────────────────────────────┴────────────────────────────┴──────────────────────────────────────────────────┘

  核心问题：领域工厂产出的高价值结构化数据（模板 + 插槽 + 实体 +
  图谱）在入库环节被"压平"为文本，丧失了结构化优势。这是一个 数据降级 问题。

  ---
  三、自我进化能力分析

  ┌──────────────┬───────────────────────────────────┬───────────────────────────────────────────────────────────────┐
  │   进化维度   │             当前状态              │                             缺失                              │
  ├──────────────┼───────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 实体库扩充   │ 实体构建器支持 AI 提取 + 去重比对 │ ETL 未识别实体无法直接流入实体构建器                          │
  ├──────────────┼───────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 模板库扩充   │ 30 个硬编码煤炭行业模板           │ 无法从已处理文档的泛化结果中学习新模板                        │
  ├──────────────┼───────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ Prompt 优化  │ prompt_config 可手动编辑          │ 没有基于提取质量（置信度）的自动调优                          │
  ├──────────────┼───────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 章节知识积累 │ SavedSection 表存在但前端无入口   │ 处理的章节结构无法沉淀和复用                                  │
  ├──────────────┼───────────────────────────────────┼───────────────────────────────────────────────────────────────┤
  │ 质量反馈闭环 │ 提取有置信度评分                  │ 置信度不影响后续流程决策，没有"低置信度 → 加强人工审核"的机制 │
  └──────────────┴───────────────────────────────────┴───────────────────────────────────────────────────────────────┘

  结论：当前设计有进化的"骨架"（实体 Schema、模板库、Prompt
  配置都是可变的），但缺少自动化的反馈回路。每个进化点都需要人工驱动，系统无法从处理历史中自主学习和改进。

  ---
  四、各子页面 UI 分析

  1. DomainFactoryView（主入口）

  问题：
  - Hero 区（"人机协同的领域知识工厂"）每次进入都占据首屏，对重复使用的操作者来说是视觉噪音
  - "Prompt 模板管理"和"领域实体构建器"只是导航按钮，放在 Hero 区下方容易被忽略
  - Tab 切换"数据源管理"和"ETL 清洗工作台"时，两个组件同时挂载但没有路由映射，无法通过 URL 直接跳转到某个 Tab

  建议：Hero 区仅首次展示或折叠；导航按钮提升为顶部标签或侧边入口；Tab 切换通过路由 query
  参数驱动（当前部分实现但不够完整）。

  2. DataSourceDashboard（数据源管理）

  合理之处：上传 → 待处理队列 → 历史记录的三段式布局清晰

  问题：
  - 上传后任务进入异步 ETL，但页面没有自动刷新机制——用户需要手动刷新才能看到状态变化
  - "查看 Markdown"按钮打开的是原始解析结果，不是结构化预览，对审核帮助有限
  - 缺少批量操作（批量重试、批量删除）

  3. EtlWorkbench（ETL 清洗工作台）

  合理之处：四步流程指示器 + 双屏校验布局

  问题：
  - 认知负担过重：4 个步骤中每步都有独立的编辑器/表格/JSON 面板，步骤之间没有强制校验——用户可以跳过步骤直接提交
  - 步骤 1（基础信息提取）：同时展示段落视图、JSON 编辑器和表单字段，信息密度极高，三个面板之间的关系不直观
  - 步骤 2（结构化数据/表格）：表格的 HTML 编辑器对非技术用户不友好，应该提供所见即所得的表格编辑
  - 步骤 3（模板泛化）：章节树 + 模板编辑 + Diff 预览，概念（"泛化""插槽""语义锚点"）对领域专家来说过于抽象
  - 步骤 4（未识别实体）：这是进化链路的关键触点，但只提供了一个简单的表格 + "保存到类型库"按钮，没有与实体构建器的联动
  - 缺少全局进度指示：用户不知道离"可提交"还差多少工作

  4. DomainEntityBuilderView（实体构建器）

  合理之处：左树右编辑的标准布局；AI 提取 + 去重比对设计良好

  问题：
  - AI 提取功能需要用户粘贴文档内容——应该支持从已上传的领域工厂任务中选择
  - 实体的"关系规则"编辑器（relation_rules）概念复杂，缺少可视化展示（如关系图预览）
  - 导入/导出是全量操作，缺少细粒度的实体版本管理和变更历史
  - 与 ETL 流程的"未识别实体"没有数据流连接

  ---
  五、核心设计问题总结

  ┌─────┬───────────────────────────────┬─────────────────────────────────────────┬─────────────────────────────────┐
  │  #  │             问题              │                  影响                   │              建议               │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │     │ 数据降级：结构化数据入库时退  │                                         │ 保留入库时的 StructuredDocument │
  │ 1   │ 化为文本                      │ 知识库检索无法利用结构化元数据          │  元数据，在 LightRAG 检索时支持 │
  │     │                               │                                         │ 按行业/报告类型/章节路径过滤    │
  ---
  五、核心设计问题总结

  ┌─────┬───────────────────────────────┬─────────────────────────────────────────┬─────────────────────────────────┐
  │  #  │             问题              │                  影响                   │              建议               │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │     │ 数据降级：结构化数据入库时退  │                                         │ 保留入库时的 StructuredDocument │
  │ 1   │ 化为文本                      │ 知识库检索无法利用结构化元数据          │  元数据，在 LightRAG 检索时支持 │
  │     │                               │                                         │ 按行业/报告类型/章节路径过滤    │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │ 2   │ 图谱只有写没有读              │ Neo4j 图谱构建了但无人消费              │ 增加 graph_query API，支持按模  │
  │     │                               │                                         │ 板/实体/章节关系查询            │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │     │ 进化回路断裂：ETL             │                                         │ 实现：未识别实体 →              │
  │ 3   │ 产出不反馈给                  │ 系统 100 份文档后能力和第 1 份时相同    │ 实体构建器、高质量泛化 →        │
  │     │ Schema/模板/Prompt            │                                         │ 模板库、低置信度提取 → Prompt   │
  │     │                               │                                         │ 调优                            │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │     │                               │ 领域工厂是"数据孤岛"，产出的知识无法被  │ 开发领域感知的 RAG 技能或 Agent │
  │ 4   │ Agent 侧无对接                │ 智能体利用                              │  middleware，能按模板/插槽/实体 │
  │     │                               │                                         │ 检索                            │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │     │ 路由断裂：section_routing_rou │                                         │ 清理 __init__.py 中的无效       │
  │ 5   │ ter 和 entity_type_router     │ 启动时可能报 import 错误                │ import，或恢复路由文件          │
  │     │ 已删除但 __init__.py 仍引用   │                                         │                                 │
  ├─────┼───────────────────────────────┼─────────────────────────────────────────┼─────────────────────────────────┤
  │     │                               │                                         │ 简化步骤：合并步骤 1+2          │
  │ 6   │ 前端 ETL 工作台认知负担高     │ 领域专家难以独立操作                    │ 为"数据校验"，步骤 3            │
  │     │                               │                                         │ 简化为模板确认/调整，步骤 4     │
  │     │                               │                                         │ 改为自动建议而非手动操作        │
  └─────┴───────────────────────────────┴─────────────────────────────────────────┴─────────────────────────────────┘

✻ Churned for 5m 59s


## 领域知识工厂的业务逻辑分析和重构建议


真实数据出来了。以下是从数据出发的完整分析：

  ---
  文档上传后 ETL 管线全链路数据流

  实际数据样本

  文档：新疆伊宁矿区北区总体规划（修编）环境影响报告书
  领域：煤炭采掘 | AI 置信度：0%

  ---
  阶段 1：解析（PARSING）— 文件 → 结构化段落

  做什么：把 Word/PDF 文件解析成 Markdown，再按章节切分为段落列表。

  产出：

  41 个段落 = 9 个标题 + 1 个表格 + 31 个正文段落

  每个段落长这样：

  [TITLE] "1 3 区域自然和社会经济概况"
  [TEXT]  "伊宁矿区北区位于伊犁盆地北缘，地势东高西低..."   ← 150字正文
  [TITLE] "1.1.2 3.1.2 气候、气象"

  人工需要校验什么：
  - 章节切分是否正确（标题层级有没有错乱）
  - 表格是否被正确识别（本例中只有 1 个表格段，但 structured_blocks 有 33 个 — 说明很多表格混在段落里）
  - 段落内容是否完整（有没有截断、乱码）

  结论：✅ 自动执行合理。人工只需抽检，不需要逐段校验。

  ---
  阶段 2：提取（EXTRACTING）— 段落 → 插槽变量值

  做什么：用 LLM 从全文中提取 149 个预定义变量的值。

  实际结果：

  form_schema 变量: 149 个
  base_info 提取值: 298 个 key
    已填充的值: 0 个  ← 全部为 None！

  这是一个严重问题。149 个变量、298 个 key，但一个值都没有提取出来。AI 置信度 0%。

  原因分析：
  - 149 个变量数量过多，LLM prompt 可能超长或被截断
  - 变量命名如 储运与运输系统 · 运输方式 包含中文和特殊符号，LLM 输出可能对不上
  - 当前任务的 AI 置信度为 0% 且全部为空，说明提取阶段实际失败了

  人工需要做什么：
  - 如果提取成功：校验每个变量的值是否正确（对照原文）
  - 如果提取失败（如本例）：需要手动填写所有值 — 149 个变量手动填是不现实的

  结论：⚠️ 当前流程是全自动一次性提取，但实际效果很差。需要考虑：是否应该分章节提取？是否应该减少变量数量？是否应该在提
  取失败时给人工一个"半自动"模式（AI 预填+人工修正）？

  ---
  阶段 3：泛化（GENERALIZING）— 原文 → 带插槽的模板

  做什么：对每个段落，AI 把具体数值替换为 {{插槽名}} 模板。

  实际结果：

  段落级泛化: 20/41 个段落有泛化结果
  全局模板: 1473 字符, 83 个插槽
  未识别插槽: 189 个

  一个泛化示例：

  原文: "伊宁矿区北区位于伊犁盆地北缘，地势东高西低..."
  泛化: "{{矿区名称}}位于{{盆地名称}}的{{方位}}，地势{{方位1}}高{{方位2}}低..."
  插槽: [矿区名称, 盆地名称, 方位, 方位1, 方位2, 方位3, 方位4,
         区域1, 特征1, 区域2, 特征2, 特征3, 地貌类型,
         最小海拔值, 最大海拔值, 最小高差, 最大高差]  ← 17 个插槽

  关键发现：

  1. 插槽过于碎片化：一句话被拆成 17 个插槽（方位1、方位2、特征1...），语义被完全拆散
  2. 189 个未识别插槽：这些插槽没有匹配到实体定义（entity_ref 全部为 None）
  3. 插槽没有值：泛化阶段只做了"替换成模板"，没有填值 — 填值是阶段 2 的事，但阶段 2 也失败了

  人工需要做什么：
  - 理论上需要校验泛化模板是否正确
  - 实际上，在插槽如此碎片化且无值的情况下，人工很难有效校验

  ---
  核心问题总结

  问题: 提取阶段形同虚设
  说明: 149 个变量全部为 None，AI 置信度 0%。人工面对 298 个空字段无从下手
  ────────────────────────────────────────
  问题: 泛化过度碎片化
  说明: 一句话 17 个插槽，语义被拆散。189 个未识别插槽，全部没有 entity_ref
  ────────────────────────────────────────
  问题: 三个阶段产出相互割裂
  说明: 提取（阶段2）产出的是 base_info 里的变量值，泛化（阶段3）产出的是段落模板+插槽。但插槽和变量之间没有对应关系 —
    插槽叫"矿区名称"，变量叫"project_name"
  ────────────────────────────────────────
  问题: 人工工作量过大
  说明: 即使提取成功，149 个变量 + 189 个未识别插槽 + 41 个段落的模板校验 = 不现实

  建议方向

  1. 从"一次性全自动"改为"分章节提取"

  当前是拿整篇文档一次性让 LLM 提取 149 个变量。应该改为：
  - 按章节分批提取（"3.1 自然环境概况"这一章只提取气候/地理相关变量）
  - 减少单次提取的变量数量（10-20 个为宜）

  2. 插槽与实体变量需要统一

  当前插槽（泛化产生）和变量（实体定义）是两套体系。需要：
  - 泛化产生的插槽应该映射到实体变量（entity_ref）
  - 或者直接基于实体变量来做泛化，而不是让 AI 自由创造插槽名

  3. ETL 工作台应该聚焦"半自动"模式

  不应期望 AI 完美提取。工作台的核心价值是：
  - 原文与提取值的对照校验（左右双屏）
  - 专家可以快速修正 AI 的错误提取
  - 对 AI 没有提取到的值，提供便捷的手动补录入口

  需要我按这个方向调整后端管线还是先重构前端工作台？


  ---

  这个段落元数据有几个明显问题：

  1. 数据冗余 — template.* 与根级字段完全重复

  template.generalized = 根级 generalized，template.slots = 根级 slots，template.original = 根级
  original，template.metadata = 根级 metadata。同一份数据存了两份，增加了存储和编辑时的同步负担。建议保留 template
  对象，根级不再重复。

  2. 插槽过度碎片化

  一句话提取了 11 个 slot，其中：
  - 海拔单位、相对高差单位 — 固定为 "m"，作为独立变量无意义，应合并到数值中
  - 地势方向 — "东高西低，北高南低" 本身是一个完整描述，拆成单一变量后模板无法表达方向组合
  - 地表特征 — "东南部地表起伏不平，东北部稍高但较为平坦" 高度特化，其他项目几乎不会复用这个值

  合理的拆法应该是 3~5 个变量：项目名称、地理位置、地势描述（合并方向+特征）、地貌类型、海拔范围（合并最小最大+单位）。

  3. metadata.chapter 与 section_path 不一致

  section_path 是 ["3", "3.1", "3.1.1"]，但 metadata.chapter 是 "3.1.2.1"，对不上。

  4. parent_title 只有编号 "3"

  应存储父章节的实际标题如 "3 区域自然和社会经济概况"，当前只有编号没有可读性。

  5. is_title: false 的段落也做了泛化

  这段是正文内容段落，不是标题。对正文做模板泛化（把每个可替换的部分都抽成 slot）会导致模板过于特化，几乎无法跨项目复用
  。泛化应该聚焦于结构化的标题型段落（如工程参数表、标准条款），而不是叙述性正文。

  ---
  总结：核心问题在后端的泛化策略 — 对叙述性正文做了过细的 slot 提取，且数据模型存在冗余。是否需要调整后端的泛化 prompt
  来控制这个行为？


  ---
   现在我已完全理解了整个流水线。接下来我将撰写分析报告。

  ---
  以下是对后端泛化策略和业务逻辑的完整分析：

  一、当前流水线架构

  文档上传
    → PARSE (Markdown → paragraphs[])
    → EXTRACT (LLM 提取 form_schema 变量)
    → GENERALIZE (两层泛化):
        1. _generate_template()  — 全局模板，从"总论/工程概况"章节生成
        2. generalize_paragraphs() — 逐段落泛化，每个 ≥20 字符的段落都调用 LLM
    → WAITING_REVIEW

  二、逐层问题分析

  问题 1：泛化范围过宽 — 无差别对所有段落泛化

  位置：domain_factory_service.py:1811-1813

  # 普通段落：文本长度至少 20 字符才处理
  if not text or len(text) < 20:
      return

  问题：只要段落 ≥20 字符就送去 LLM 泛化，不分段落类型。对叙述性正文（如地形描述、环境现状描述）做泛化，产出的模板高度特
  化、几乎无法复用。以用户数据为例，一段 93 字的地形描述被拆出 11 个 slot，其中 海拔单位、相对高差单位
  这类固定值被当作独立变量。

  优化建议：只对标题型段落（is_title=True）和含数值/参数的段落做泛化。叙述性正文跳过。

  # 建议：增加段落类型筛选
  is_title = para.get("is_title", False)
  has_numeric = bool(re.search(r'\d+(?:\.\d+)?', text))

  # 标题型段落 或 含数值参数的段落才泛化
  if not is_title and not has_numeric:
      return

  # 纯叙述性段落（无数字、无标题）跳过

  问题 2：Prompt 缺乏粒度控制指导

  位置：domain_factory_service.py:235-269（默认 template prompt）

  当前 Prompt 只说了"用双层大括号表示可替换变量"，但没有告诉 LLM 什么粒度该提取、什么该保留为原文。导致 LLM
  倾向于过度拆分 — 把每个可辨识的语义片段都变成 slot。

  优化建议：在 Prompt 中增加粒度约束：

  泛化规则：
  1. 只提取具有跨项目复用价值的变量（项目名称、地理位置、数值参数等）
  2. 以下内容不提取为变量，保持原文：
     - 固定单位（m、Mt/a、年）直接保留，不作为独立 slot
     - 描述性短语（如"东南部地表起伏不平"）保持原文，不拆分为变量
     - 方向性描述（如"东高西低"）保持原样
  3. 每个段落提取的 slot 数量建议不超过 5 个
  4. 优先合并相关变量：如"海拔范围 630～1200m"作为一个变量，不拆分最小值/最大值/单位

  问题 3：数据冗余 — template.* 与根级字段完全重复

  位置：domain_factory_service.py:786-797

  # 写入 para["template"] 对象
  para["template"] = {
      "generalized": gen_result.get("generalized", ""),
      "original": para.get("content", ""),
      "slots": raw_slots,
      "metadata": gen_result.get("metadata", {}),
  }
  # 又写入根级字段 — 完全重复
  para["original"] = para.get("content", "")
  para["generalized"] = gen_result.get("generalized", "")
  para["slots"] = raw_slots
  para["metadata"] = gen_result.get("metadata", {})

  这导致每段 JSON 体积翻倍。用户数据中 11 个 slot 被存了两份。

  优化建议：只保留 para["template"] 对象，删除根级重复字段。前端统一从 template
  中读取。如果需要向后兼容，可以在读取时展平，而不是在存储时重复。

  问题 4：全局模板 + 段落模板职责重叠

  _generate_template()（全局模板）从"总论/工程概况"截取 ≤2000 字做一次泛化，generalize_paragraphs()
  又对同一段落再做一次。两者使用几乎相同的 Prompt，产生重复结果且浪费 LLM 调用。

  优化建议：
  - 全局模板（_generate_template）：仅生成文档级摘要模板，覆盖项目名称、产能、位置等核心参数，slot 控制在 10 个以内
  - 段落模板（generalize_paragraphs）：只处理全局模板未覆盖的参数型段落（含数值、工程指标等），跳过纯文字描述

  问题 5：parent_title 只存编号不存标题

  位置：domain_factory_service.py:1074

  "parent_title": section_path_stack[0] if section_path_stack else "",

  section_path_stack[0] 是 "3"，不是 "3
  区域自然和社会经济概况"。后续模板匹配和实体关联都依赖这个字段做上下文判断，只有编号缺少语义。

  优化建议：在构建段落时，用已解析的标题段落回填 parent_title：

  # 维护一个 level -> title 的映射
  title_map = {}
  for para in paragraphs:
      if para.get("is_title") and para.get("section_path"):
          level = len(para["section_path"])
          title_map[tuple(para["section_path"])] = para["title"]

  # 构建段落时回填
  parent_path = tuple(section_path_stack[:-1]) if len(section_path_stack) > 1 else ()
  parent_title = title_map.get(parent_path, section_path_stack[0] if section_path_stack else "")

  问题 6：metadata.chapter 与 section_path 不一致

  用户数据中 section_path = ["3", "3.1", "3.1.1"] 但 metadata.chapter = "3.1.2.1"。这是因为 metadata.chapter 由 LLM
  自行生成，没有约束它与 section_path 一致。

  优化建议：在 Prompt 中明确约束：

  metadata.chapter 必须与输入的 chapter_hint 值完全一致，不要自行推断。

  并在后端做后校验：如果 LLM 返回的 chapter 与 chapter_hint 不同，用 chapter_hint 覆盖。

  问题 7：fallback 策略过于粗暴

  位置：domain_factory_service.py:2295-2304

  "generalized": re.sub(r"\d+(?:\.\d+)?", "{{数值}}", text, count=1),

  泛化失败时，把第一个数字替换成 {{数值}}，产生一个无意义的 fallback。如果段落没有数字则 fallback 结果与原文一样。建议
  fallback 直接标记为"未泛化"而不是假装泛化了。

  ---
  三、优化优先级建议

  ┌────────┬─────────────────────────────────────────────────┬───────────────────────────────────┐
  │ 优先级 │                      改动                       │               影响                │
  ├────────┼─────────────────────────────────────────────────┼───────────────────────────────────┤
  │ P0     │ 泛化前筛选段落类型（跳过叙述性正文）            │ 减少 ~60% 无效 LLM 调用，提升质量 │
  ├────────┼─────────────────────────────────────────────────┼───────────────────────────────────┤
  │ P0     │ Prompt 增加粒度约束（合并单位、限制 slot 数量） │ 直接改善泛化质量                  │
  ├────────┼─────────────────────────────────────────────────┼───────────────────────────────────┤
  │ P1     │ 去除数据冗余（只保留 template 对象）            │ 减少 ~40% 存储体积                │
  ├────────┼─────────────────────────────────────────────────┼───────────────────────────────────┤
  │ P1     │ 回填 parent_title 为实际标题而非编号            │ 改善模板匹配准确度                │
  ├────────┼─────────────────────────────────────────────────┼───────────────────────────────────┤
  │ P2     │ 统一 metadata.chapter 与 section_path           │ 消除数据不一致                    │
  ├────────┼─────────────────────────────────────────────────┼───────────────────────────────────┤
  │ P2     │ 全局模板与段落模板职责分离                      │ 避免重复调用                      │
  └────────┴─────────────────────────────────────────────────┴───────────────────────────────────┘

  需要我开始实施哪些优化？


---

旧样例报告中有哪些可复用资产

  第一层：文档骨架（复用价值最高）

  章节目录大纲 — 环评报告受行业规范约束，同领域内的章节结构高度固定。煤炭采掘类报告几乎都有：
  1 总论
  2 工程概况
  3 区域自然和社会经济概况
  4 环境质量现状
  5 环境影响预测与评价
  6 环境保护措施
  7 环境管理与监测
  8 结论

  一个新项目不需要重新构思"写什么章节"，直接复用大纲即可。但当前 pipeline 完全丢弃了这个结构，只把它当成导航用的
  section_path。

  段落骨架 — 每个章节内部也有固定的段落模式。比如"工程概况"章节，几乎每份报告都按这个顺序叙述：
  项目名称和位置 → 建设性质（新建/改扩建） → 设计产能 → 服务年限 →
  井田范围 → 开拓方式 → 采煤方法 → ...

  这是领域知识，不是某个具体项目的特征。

  第二层：句式模板（复用价值高）

  环评报告中有大量"填空式"句式，结构固定、只替换具体值：

  - {{项目名称}}位于{{行政区划}}，地理坐标为东经{{经度}}°～{{经度2}}°，北纬{{纬度}}°～{{纬度2}}°
  - 矿井设计生产能力{{产能数值}}{{产能单位}}，服务年限{{服务年限}}年
  - 经预测，{{预测场景}}，{{预测指标}}最大值为{{最大值}}{{单位}}，位于{{位置描述}}

  这些句式模板的结构本身就是核心资产，而当前的泛化策略把重点放在了"提取了哪些 slot"上，忽略了模板结构本身的复用价值。

  第三层：标准条款（可直接复用）

  环评报告中有大量引用法规标准的原文，这些内容跨项目几乎不变：

  - "根据《建设项目环境影响评价分类管理名录》..."
  - "本项目执行《煤炭工业污染物排放标准》(GB 20426-2006) 中..."
  - "对照《环境影响评价技术导则 大气环境》(HJ 2.2-2018)..."

  这些段落不需要泛化，不需要提取 slot，直接作为标准文本库复用即可。

  第四层：表格结构模板

  环评中的表格（敏感目标清单、监测点位表、预测结果表等）有固定的列定义：

  敏感目标表：村庄名称 | 方位 | 距离(km) | 户数 | 影响因素
  监测点表：点位编号 | 点位名称 | 监测因子 | 监测频次 | 执行标准

  复用的是列定义（schema），不是具体数据。当前 pipeline 对表格只是转成 HTML 存储，没有提取表格的 schema 模板。

  第五层：逻辑关系和写作范式

  - 因果链："因为项目位于 XX 区域 → 地质条件为 XX → 因此需采用 XX 开采工艺"
  - 条件分支："若项目属于 XX 类别 → 需编制 XX 专项评价"
  - 数据引用链：监测数据表 → 现状分析段落 → 预测参数 → 结论性判断

  这些是写作方法论，是最高层级的复用资产。

  ---
  对当前 Pipeline 的反思

  回到当前实现，问题就很明显了：

  ┌───────────────────────────────────┬──────────────────────────────────────────────────────────────────┐
  │             当前做法              │                            应该的做法                            │
  ├───────────────────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ 对每个 ≥20 字段落无差别泛化       │ 先分类：标准条款直接入库、叙述性正文跳过、参数型段落才做模板泛化 │
  ├───────────────────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ 泛化目标 = 把所有值替换成 slot    │ 泛化目标 = 提取句式模板结构，保留固定的修饰语，只替换真正的变量  │
  ├───────────────────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ 全局模板 + 段落模板做两遍同样的事 │ 全局提取文档骨架，段落级只处理已分类的模板型段落                 │
  ├───────────────────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ 表格当 HTML 存                    │ 表格提取 schema 模板（列定义），数据作为填充样例                 │
  ├───────────────────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ 泛化结果与段落绑定                │ 模板应独立于源文档，存入模板库，按领域/章节类型索引              │
  ├───────────────────────────────────┼──────────────────────────────────────────────────────────────────┤
  │ 11 个 slot 碎片化                 │ 模板控制在 3~5 个核心变量，粒度对齐实际报告编写需求              │
  └───────────────────────────────────┴──────────────────────────────────────────────────────────────────┘

  建议的 Pipeline 重设计思路


  建议的 Pipeline 重设计思路

  文档上传
    → PARSE (解析段落 + 识别章节结构)
    → CLASSIFY (段落分类):
        ├─ 标题型 → 提取章节大纲骨架
        ├─ 标准条款 → 标记为"可复用原文"，直接入库
        ├─ 参数型段落 → 泛化为句式模板（提取 3~5 个核心 slot）
        ├─ 表格型 → 提取表格 schema（列定义模板）
        └─ 叙述性正文 → 标记为参考素材，不做泛化
    → TEMPLATE LIBRARY (模板入库，按 领域×章节类型 索引)
    → WAITING_REVIEW (人工校验分类结果和模板质量)

  核心变化：先分类再泛化，而不是无差别泛化后再人工筛。

  ---

   分析一：文档骨架的图谱构建

  当前做法

  当前入库时分两层构建：

  LightRAG（向量库）：将正文、模板、章节、插槽作为独立的 embedding chunk 写入，通过语义相似度检索。章节信息以 [SECTION]
  标签写入向量库，检索时靠文本相似度匹配。

  Neo4j（图谱库）：由 GraphBuilder 构建如下结构：
  Document -[HAS_SECTION]-> Section -[HAS_CHILD]-> Section
                             -[NEXT_SECTION]-> Section（同级顺序）
                             -[COMPOSED_OF {order}]-> ParagraphTemplate
                                                       -[HAS_SLOT]-> Slot
                                                                               -[CONSTRAINS]-> EntitySchema

  当前方案的问题

  1. 骨架绑定在单篇文档上，无法跨文档聚合

  每个 Section 节点的 ID 是 sec_{doc_id}_{hash(section_path)}，即每篇文档有自己的独立骨架。入库 10
  份煤炭环评报告，会产生 10 套几乎相同的 1 总论 / 2 工程概况 / ... 骨架，但它们之间没有关联。

  写新报告时想查"煤炭环评的章节大纲"，需要：
  - 要么检索某个 Document 然后遍历其 HAS_SECTION 子图（只看到一份报告的骨架）
  - 要么聚合所有 Document 的 Section 后去重（Neo4j 没有天然支持）

  2. Section 节点缺少领域级的元数据

  Section 只记录了 title、level、section_path，没有标记这个章节的类型语义：
  - "3 区域自然和社会经济概况" 是"环境背景描述"型章节
  - "5 环境影响预测与评价" 是"技术分析"型章节
  - "6 环境保护措施" 是"对策建议"型章节

  这些语义标签决定了新项目是否需要这个章节、用哪些模板填充。

  3. 章节骨架和段落模板的关联是 1:N 的扁平关系

  当前 Section -[COMPOSED_OF]-> ParagraphTemplate
  只记录了顺序（order），没有记录段落在章节中的角色（开头总述？参数说明？总结判断？）。新报告编写时，知道"需要写 3.1
  自然环境概况"是不够的，还需要知道这一节内部应该怎么组织段落。

  优化建议：领域骨架模板

  新增节点类型：DomainOutline（领域大纲模板）

  DomainOutline -[HAS_CHAPTER]-> ChapterTemplate -[HAS_CHILD]-> ChapterTemplate
                                                   -[NEXT_CHAPTER]-> ChapterTemplate
                                                   -[REQUIRES_PARAGRAPH_ROLE {role, order}]-> ParagraphRole

  ParagraphRole：
    - role: "总述" | "参数说明" | "数据引用" | "标准引用" | "判断结论"
    - template_hints: 推荐使用的段落模板类型

  构建时机：当同一领域入库 ≥2 份文档后，自动合并骨架：
  1. 按 section_path 对齐所有文档的 Section
  2. 出现频率 ≥50% 的章节视为领域必备章节
  3. 统计每个章节内段落模板的出现频率，提取高频模板作为推荐

  写入时检索：写新报告时，直接从 DomainOutline 出发，一层层展开 HAS_CHAPTER，得到完整的章节骨架 +
  每章推荐的段落模板，不需要从向量库做语义搜索。

  ---
  分析二：标准条款的适用性筛选

  问题本质

  标准条款不是简单的"可直接复用"或"不能复用"二元判断，而是一个适用性谱系：

  ┌───────────────────┬───────────────────────────────────────┬────────────────────────┐
  │     适用范围      │                 示例                  │        复用策略        │
  ├───────────────────┼───────────────────────────────────────┼────────────────────────┤
  │ 国家标准（GB/HJ） │ HJ 2.2-2018 大气导则                  │ 所有项目通用，直接复用 │
  ├───────────────────┼───────────────────────────────────────┼────────────────────────┤
  │ 行业标准（MT/AQ） │ MT/T 5016-96 煤矿设计规范             │ 同行业项目复用         │
  ├───────────────────┼───────────────────────────────────────┼────────────────────────┤
  │ 地方标准（DB）    │ DB61/XXX 陕西省煤炭工业污染物排放标准 │ 仅同省份项目复用       │
  ├───────────────────┼───────────────────────────────────────┼────────────────────────┤
  │ 项目级规范        │ 某矿环评批复文件中的特定要求          │ 仅该项目适用           │
  └───────────────────┴───────────────────────────────────────┴────────────────────────┘

  当前 pipeline 的问题

  当前泛化阶段把标准条款和参数型段落同等对待 — 都调用同一个 LLM 泛化 prompt，产出类似的 slot
  模板。但标准条款的核心价值不是"把某个标准编号变成 slot"，而是：
  - 知道在哪个章节位置需要引用哪类标准
  - 标准条款的适用条件是什么（行业、地区、项目规模）

  优化建议：标准条款分层处理

  Step 1 — 段落分类阶段识别标准引用

  在 CLASSIFY 阶段（前面讨论的新增步骤）增加一个分类：

  def classify_paragraph(para):
      content = para.get("content", "")

      # 标准引用模式匹配
      standard_pattern = re.compile(
          r'(GB|HJ|MT|AQ|DB|CJJ|JTG|TB|DL)\s*\d+[\d\-]*',
          re.IGNORECASE
      )
      if standard_pattern.search(content) and ("标准" in content or "规范" in content or "导则" in content):
          return "standard_clause"

      # ... 其他分类

  Step 2 — 提取标准元数据

  对识别为标准引用的段落，提取结构化元数据而非泛化模板：

  {
    "type": "standard_clause",
    "standard_code": "HJ 2.2-2018",
    "standard_name": "环境影响评价技术导则 大气环境",
    "scope": "national",          // national / industry / regional / project
    "applicable_region": null,    // null 表示全国，"陕西省" 表示地方标准
    "applicable_industry": null,  // null 表示全行业，"煤炭采掘" 表示行业专用
    "chapter_position": "5.2",    // 在报告中的引用位置
    "original_text": "根据《环境影响评价技术导则 大气环境》(HJ 2.2-2018)...",
    "condition": "项目涉及大气环境影响评价时引用"
  }

  Step 3 — 图谱中构建标准引用关系

  新增节点：StandardClause

  ChapterTemplate -[REQUIRES_STANDARD {condition}]-> StandardClause

  StandardClause 属性：
    - code: "HJ 2.2-2018"
    - name: "环境影响评价技术导则 大气环境"
    - scope: "national"
    - applicable_region: null
    - applicable_industry: null

  Step 4 — 写新报告时筛选

  写新项目报告时，根据项目上下文（行业、省份、项目类型）过滤标准引用：

  // 查询某领域某章节需要的标准条款，按适用范围过滤
  MATCH (ch:ChapterTemplate {domain: $domain})-[r:REQUIRES_STANDARD]->(std:StandardClause)
  WHERE std.scope = 'national'
     OR (std.scope = 'industry' AND std.applicable_industry = $industry)
     OR (std.scope = 'regional' AND std.applicable_region = $region)
  RETURN std, r.condition
  ORDER BY std.scope  // 国家标准优先

  对于 scope = 'project' 的条款，不入图谱标准库，只保留在源文档的结构化数据中作为参考。

  ---
  总结：入库流水线建议调整

  文档上传
    → PARSE (解析段落)
    → CLASSIFY (段落分类):
        ├─ 标题型 → 提取章节骨架（写入 DomainOutline）
        ├─ 标准引用 → 提取标准元数据（scope / region / industry）
        ├─ 参数型段落 → 泛化为句式模板（限制 3~5 slot）
        ├─ 表格型 → 提取表格 schema
        └─ 叙述性正文 → 标记为参考素材
    → INGEST:
        ├─ LightRAG：正文 embedding（保持现有）
        ├─ Neo4j：
        │   ├─ DomainOutline（跨文档合并骨架）
        │   ├─ ChapterTemplate → StandardClause（带适用性过滤）
        │   ├─ ParagraphTemplate → Slot（现有）
        │   └─ Section（保留为文档级，关联到 DomainOutline）
        └─ 不再对所有段落无差别泛化

  需要继续讨论哪个方向，或者可以开始设计具体的技术方案了？

  ---

  我现在对情况有了全面的了解。接下来我将整理分析报告。

  ---
  从这份样例报告中，可以看到标准引用在两个完全不同的场景中出现：

  场景一：编制依据（1.2 节）— 清单式罗列

  这是结构最清晰的部分，文档已经按层级自组织好了：

  ┌──────────────────┬─────────┬──────────────────────────────────────────────────────┬────────┐
  │       层级       │  章节   │                         示例                         │  数量  │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 法律             │ 1.2.2.1 │ 《中华人民共和国环境保护法》                         │ 19 部  │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 行政法规         │ 1.2.2.2 │ 《规划环境影响评价条例》，国务院令第559号            │ 8 部   │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 地方性法规       │ 1.2.2.3 │ 《宁夏回族自治区环境保护条例》                       │ 7 部   │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 国务院部门规章   │ 1.2.3.1 │ 《煤炭矿区总体规划管理暂行规定》，国家发改委令第14号 │ 31 件  │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 地方政府规章     │ 1.2.3.2 │ 《宁夏回族自治区"十三五"节能减排综合工作方案》       │ 9 件   │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 技术规范（导则） │ 1.2.4   │ HJ130-2019、HJ2.2-2018、GB50215-2015                 │ 16 项  │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 相关规划         │ 1.2.5   │ 国家/地方层次规划                                    │ ~35 件 │
  ├──────────────────┼─────────┼──────────────────────────────────────────────────────┼────────┤
  │ 技术资料         │ 1.2.6   │ 项目级地质报告、环评报告                             │ 21 件  │
  └──────────────────┴─────────┴──────────────────────────────────────────────────────┴────────┘

  这一节的特点：格式高度统一，有编号、有名称、有文号、有施行日期。结构化提取很容易，用正则就能搞定。但这一节的价值不在于
  "内容复用"，而在于告诉新项目该在哪些位置检索和引用哪些层级的文件。

  场景二：正文引用 — 语义嵌入

  正文中的标准引用才是真正需要处理的部分，它们以三种模式出现：

  模式 A：适用性判定型
  矿区属环境空气质量二类区。
  → 依据《环境空气质量功能区划分原则与技术方法》（HJ14-1996）

  厂界噪声满足 GB12348-2008《工业企业厂界噪声标准》中3类标准要求。
  → 标准编号 + 标准名称 + 适用类别

  模式 B：合规性验证型
  烟气治理后排放应符合《锅炉大气污染物排放标准》（GB13271-2014）燃气锅炉排放标准。
  水洞沟水质不满足《地表水环境质量标准》（GB3838-2002）IV类标准。

  模式 C：分类定级型
  按MT/T803-1999标准分级，除丁家梁井田包含二级含砷煤外，其余各矿砷含量均属一级含砷煤
  按GB/T15224.2-2010标准分级
  根据《中国土壤分类与代码》(GB/T17296-2009)

  核心发现：适用性是一个多维筛选问题

  这份报告是一份宁夏煤炭矿区规划环评，所以引用了大量宁夏地方标准和银川市级文件。一份陕西的煤矿环评，能复用的是：

  ┌───────────────┬──────────────────────────────────────────┬───────────────────────────────────────┐
  │     维度      │                  能复用                  │               不能复用                │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 法律          │ 环保法、环评法、各污染防治法（全国统一） │ —                                     │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 行政法规      │ 国务院条例（全国统一）                   │ —                                     │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 地方性法规    │ —                                        │ 宁夏回族自治区条例 → 需换为陕西省条例 │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 部门规章      │ 发改委令、环发/环办文件（全国统一）      │ —                                     │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 地方政府规章  │ —                                        │ 银川市行动计划 → 需换为对应城市文件   │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 技术规范      │ HJ 导则、GB 国标（全国统一）             │ —                                     │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 相关规划-国家 │ 全国性规划（通用）                       │ —                                     │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 相关规划-地方 │ —                                        │ 宁夏十三五规划、灵武市规划            │
  ├───────────────┼──────────────────────────────────────────┼───────────────────────────────────────┤
  │ 技术资料      │ —                                        │ 项目级报告（完全不可复用）            │
  └───────────────┴──────────────────────────────────────────┴───────────────────────────────────────┘

  所以标准条款的筛选维度应该是 4 个：

  scope:       national | regional | project        // 适用范围
  authority:   全国人大 | 国务院 | 部委 | 省级 | 市级       // 制定机关
  domain:      煤炭采掘 | 石油化工 | 交通运输              // 行业领域
  document_type: 法律 | 行政法规 | 地方性法规 | 部门规章 | 地方规章 | 技术规范 | 规划文件 | 项目资料  // 文件类型

  新项目编写时，根据 scope=national 或 scope=regional AND authority matches 项目所在地 来筛选。

  处理策略建议

  编制依据部分（场景一）：不需要泛化，直接按层级结构化提取。每条记录为一个 LegalReference 节点：

  {
    "name": "中华人民共和国环境保护法",
    "code": null,
    "type": "法律",
    "authority": "全国人大",
    "scope": "national",
    "effective_date": "2015-01-01",
    "chapter_position": "1.2.2.1"
  }

  图谱关系：
  ChapterTemplate[编制依据] -[REQUIRES_REF {layer: "法律"}]-> LegalReference

  新项目生成时，layer=法律 的节点直接复用，layer=地方性法规 的节点按 scope + authority 过滤替换。

  正文引用部分（场景二）：不需要泛化为模板，而是提取为标准应用记录：

  {
    "standard_code": "GB13271-2014",
    "standard_name": "锅炉大气污染物排放标准",
    "application_type": "compliance",      // applicability | compliance | classification
    "context": "锅炉房烟气治理后排放应符合...燃气锅炉排放标准",
    "chapter_position": "4.6.2",
    "scope": "national"
  }

  图谱关系：
  ChapterTemplate[环境保护措施-大气] -[APPLIES_STANDARD {usage: "compliance"}]-> LegalReference

  新项目编写时，从 ChapterTemplate 出发，查询该章节需要应用哪些标准，然后按项目上下文过滤。

  ---
  这两部分的处理方式完全不同于当前的"泛化为模板"策略。核心思路是：编制依据 = 结构化提取入图谱，正文引用 =
  关联到章节模板的标准应用记录。要不要继续细化这个方案的技术实现？


  ---

  同行业不同报告类型的对比

  仅煤炭采掘行业，常见的报告类型就有：

  ┌────────────────────────────────┬────────────────────────┬────────────────────┬──────────────────────────┐
  │            报告类型            │      章节3 写什么      │      核心标准      │       编制依据侧重       │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 环境影响评价报告               │ 区域自然和社会经济概况 │ HJ 2.1~2.4, HJ 619 │ 环保法、污染防治法       │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 固体矿物地质勘查报告           │ 矿区地质构造特征       │ GB/T 13908         │ 矿产资源法、地质勘查规范 │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 矿井设计报告                   │ 井田开拓与采煤方法     │ GB 50215           │ 煤炭法、安全生产法       │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 安全评价报告                   │ 危险有害因素分析       │ AQ 标准            │ 安全生产法、煤矿安全规程 │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 可行性研究报告                 │ 市场分析与建设规模     │ 发改投资规范       │ 投资相关法规             │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 矿区总体规划                   │ 规划方案与开发时序     │ GB 50465           │ 规划环评条例、发改委规章 │
  ├────────────────────────────────┼────────────────────────┼────────────────────┼──────────────────────────┤
  │ 矿山地质环境保护与恢复治理方案 │ 地质灾害危险性评估     │ DZ/T 标准          │ 地质灾害防治条例         │
  └────────────────────────────────┴────────────────────────┴────────────────────┴──────────────────────────┘

  即使是同一个项目（同一个煤矿），写环评报告和写地质勘查报告，章节结构、引用标准、段落模板完全不同。

  当前系统的缺失

  看当前数据模型中的 domain 维度：

  # domain_factory_api.js
  const defaultDomains = [
    { id: 'coal', code: 'coal', name: '煤炭采掘', description: '煤矿/露天矿环评项目' },
    { id: 'chem', code: 'chem', name: '石油化工', description: '化工/精细化工环评项目' },
    { id: 'transport', code: 'transport', name: '交通运输', description: '交通工程与物流园项目' }
  ]

  描述里写了"环评项目"，但 domain 只有行业维度。图谱和模板也没有 report_type 的区分：

  当前：DomainOutline(coal) → ChapterTemplate → ParagraphTemplate

  问题就暴露了：
  1. 上传一份煤炭地质勘查报告，系统按 coal 领域入库，但图谱里混入了地质勘查的章节骨架，和环评报告的骨架混在一起
  2. 写新报告时查询 coal 领域的模板，返回的是环评+地质勘查混合的结果
  3. 标准引用也混了 — 煤炭行业的安全标准出现在环评报告的模板中

  应该怎么改

  维度定义

  领域(domain) × 报告类型(report_type) = 一个独立的模板空间

  DomainOutline {
    domain: "coal"
    report_type: "eia_report"          // 环境影响评价报告
  }

  DomainOutline {
    domain: "coal"
    report_type: "geological_survey"    // 地质勘查报告
  }

  DomainOutline {
    domain: "coal"
    report_type: "mine_design"          // 矿井设计报告
  }

  报告类型体系

  环评领域本身就有细分：

  报告类型(report_type):
  ├── eia_report                   环境影响评价报告
  │   ├── eia_construction           建设项目环评
  │   ├── eia_planning               规划环评（如这份横城矿区总体规划环评）
  │   └── eia_post_assessment        环境影响后评价
  ├── geological_report            地质勘查报告
  │   ├── geological_exploration     地质勘查报告
  │   └── geological_hazards         地质灾害评估报告
  ├── safety_assessment            安全评价报告
  ├── feasibility_study            可行性研究报告
  ├── mine_design                  矿井设计报告
  ├── land_reclamation             土地复垦方案
  ├── water_soil_conservation      水土保持方案
  └── emergency_plan               突发环境事件应急预案

  图谱结构调整

  DomainOutline(domain="coal", report_type="eia_report")
    -[HAS_CHAPTER]-> ChapterTemplate(title="1 总论")
                       -[HAS_CHILD]-> ChapterTemplate(title="1.2 编制依据")
                                         -[REQUIRES_REF {layer:"法律"}]-> LegalReference
                                         -[REQUIRES_REF {layer:"行政法规"}]-> LegalReference
                                         -[REQUIRES_REF {layer:"技术规范"}]-> LegalReference
                       -[HAS_CHILD]-> ChapterTemplate(title="1.3 评价目的与原则")
    -[HAS_CHAPTER]-> ChapterTemplate(title="3 区域自然和社会经济概况")
                       -[REQUIRES_STANDARD {usage:"applicability"}]-> StandardClause(code="HJ14-1996")
                       -[COMPOSED_OF]-> ParagraphTemplate

  写新报告时：

  // 查询"煤炭行业环评报告"的章节骨架
  MATCH (outline:DomainOutline {domain: "coal", report_type: "eia_report"})
        -[HAS_CHAPTER]->(ch:ChapterTemplate)
  RETURN ch
  ORDER BY ch.level, ch.order

  // 查询该骨架下某个章节需要的法律依据（按项目所在地过滤）
  MATCH (ch:ChapterTemplate {title: "编制依据"})
        -[r:REQUIRES_REF]->(ref:LegalReference)
  WHERE ref.scope = 'national'
     OR (ref.scope = 'regional' AND ref.applicable_region CONTAINS $province)
  RETURN ref, r.layer AS layer
  ORDER BY layer, ref.authority_level

  对 Pipeline 的影响

  上传时需要指定 report_type（目前只选 domain）：

  上传文档 → 选择领域(coal) + 报告类型(eia_report)
           → CLASSIFY 时按 report_type 差异化处理：
              eia_report → 识别"编制依据"章节，分层提取法律引用
              geological_report → 识别"矿区地质"章节，提取地层/构造信息
              → 图谱按 (domain, report_type) 空间写入

  前端 DomainFactoryView 的领域选择器需要改为两级选择：

  当前：[煤炭采掘] [石油化工] [交通运输]

  建议：行业：[煤炭采掘 ▼]
        类型：[环境影响评价报告 ▼] [地质勘查报告] [矿井设计报告] ...

  对样例分析的回溯

  以这份"横城矿区总体规划环评报告书"为例，正确的入库标签应该是：

  domain: "coal"
  report_type: "eia_planning"       // 规划环评（不是建设项目环评）

  它的 1.2 节编制依据中有"规划环境影响评价条例"、"煤炭矿区总体规划管理暂行规定"等，这些都是规划环评特有的引用。如果
  label 只标到 eia_report，就会和建设项目环评的编制依据混在一起。所以 report_type 的粒度也要够细。

  ---
  总结：domain（行业）是横向维度，report_type（报告类型）是纵向维度，两者交叉才能定位到正确的模板空间。这个改动涉及数据
  库模型、图谱结构、前端选择器、Pipeline 分类逻辑，要动的地方不少。要不要把这个作为下一阶段的整体方案来规划？